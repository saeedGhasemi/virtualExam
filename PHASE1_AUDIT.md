# گزارش کنترل کیفیت فاز اول — MetaQuizy (سامانه آزمون مجازی)

تاریخ بررسی: ۱۴۰۵/۰۵/۲۹ (۲۰۲۶-۰۸-۲۰)
نوع این مرحله: **فقط بررسی (Read-only Audit)** — هیچ کد یا فایلی در این مرحله تغییر داده نشده است.

---

## ۱. خلاصه مدیریتی

پروژه یک اپلیکیشن **Django 5.1 تک‌اپی** (`apps/core`) با دیتابیس SQLite/PostgreSQL، Tailwind CSS + Bootstrap برای استایل، و بدون DRF/API جدا (همه‌چیز از طریق ویوهای کلاسیک Django render می‌شود) است.

مهم‌ترین و **بحرانی‌ترین یافته**: پروژه هم‌اکنون **دو لایه داده کاملاً موازی و ناسازگار** دارد که با هم هماهنگ نیستند:

1. **لایه ORM جنگو** (`apps/core/models.py`, migrations `0001` تا `0013`): مدل‌های استاندارد جنگو مثل `SystemRole`, `UserProfile`, `TeacherProfile`, `StudentProfile`, `Course`, `Exam`, `Question` و غیره.
2. **لایه «ERD» با SQL خام** (ساخته‌شده در `migrations/0014_create_erd_tables.py` با `CREATE TABLE IF NOT EXISTS` مستقیم، خارج از مدیریت ORM): جدول‌هایی مثل `profiles`, `org_units`, `teacher_profiles`, `student_profiles`, `academic_manager_profiles`, `academic_manager_scopes`, `exams`, `questions`, `student_groups`, `exam_attempts`, `attempt_answers`, `academic_terms`, `system_settings` و... که فقط از طریق توابع کمکی دستی (`erd_row`, `erd_rows`, `erd_count`, `erd_execute` در `views.py`) با SQL خام صدا زده می‌شوند.

این دو سیستم **به هم متصل نیستند** (نه Foreign Key، نه همگام‌سازی داده). ارتباط کاربر جنگو (`request.user`) به رکورد جدول خام `profiles` صرفاً با **تطبیق رشته‌ای username/email** انجام می‌شود (`erd_profile_for_user`, خط ۷۲۲ `views.py`)، نه کلید خارجی. اگر کاربری در جدول `profiles` رکورد نداشته باشد، `erd_primary_role` به‌صورت **پیش‌فرض نقش «student» را برمی‌گرداند** (خط ۷۵۰-۷۶۴) به‌جای رد دسترسی — این یک **ریسک امنیتی جدی** است (نه یک خطای بی‌ضرر).

نتیجه عملی: بخش‌هایی از پنل مدیر آموزشی (آخرین نسخهٔ تعریف‌شدهٔ `exam_manager_dashboard` و توابع مشابه) از لایهٔ SQL خام استفاده می‌کنند، درحالی‌که پنل مدیر سیستم (`super_admin_*`) و مدیر مؤسسه (`institution_admin_*`) هنوز از ORM استاندارد استفاده می‌کنند. یعنی **دو معماری داده هم‌زمان در حال اجرا هستند** و کار تکمیل فاز اول باید ابتدا دربارهٔ انتخاب یکی از این دو مسیر تصمیم بگیرد (به بخش «نیازمند تصمیم» مراجعه شود).

یافتهٔ بحرانی دوم: `apps/core/views.py` **۱۶۵۱۵ خط** است و شامل **ده‌ها تعریف تابع تکراری هم‌نام** است (به بخش ۵ مراجعه شود) — در پایتون فقط آخرین تعریف فعال می‌ماند، پس بخش زیادی از این فایل **کد کاملاً مرده و غیرقابل‌دسترس** است که خواندن/نگهداری پروژه را بسیار دشوار کرده.

---

## ۲. ساختار پروژه و پشته فناوری

- **بک‌اند:** Django 5.1.9، psycopg (Postgres) با سقوط به SQLite در dev، gunicorn+whitenoise برای production. تنها یک اپ جنگو ثبت شده: `apps.core` (`INSTALLED_APPS` در `config/settings.py`).
- **بدون DRF** — هیچ REST API جداگانه‌ای وجود ندارد؛ فرم‌ها با POST معمولی جنگو ارسال می‌شوند.
- **فرانت‌اند:** Tailwind CSS (build شده در `static/dist/styles.css`, gitignore شده) + Bootstrap 5 (vendor شده در `static/vendor`) + jQuery. کامپوننت تعاملی خاصی (React/Vue) وجود ندارد.
- **پوشهٔ `virtual-exam-starter/`**: یک **پروژهٔ کاملاً جدا و بلااستفاده** (Django + DRF + React/Vite با `docker-compose`, `Pipfile`) که هیچ ارتباطی با اپ اصلی ندارد و هیچ‌جا import/reference نمی‌شود. این یک **starter/boilerplate قدیمی یا آزمایشی رهاشده** به نظر می‌رسد (به بخش ۶ مراجعه شود).
- **دیتابیس migrations:** ۲۲ فایل migration تا `0022_question_builder_fields.py`. `python manage.py check` بدون خطا اجرا شد (سازگاری پایه ORM برقرار است) اما این تست شامل جدول‌های ERD خام (که خارج از state ORM هستند) نمی‌شود.
- **تست:** هیچ فایل `tests.py` یا پوشهٔ تست در `apps/core` وجود ندارد. **صفر پوشش تست.**
- **Lint/CI:** هیچ پیکربندی flake8/ruff/black/eslint یافت نشد. فقط اسکریپت‌های `build:css` / `watch:css` در `package.json`.
- **آلودگی مخزن:** دو پوشهٔ حجیم untracked در ریشهٔ ریپو — `.chrome-auth-shot-register/` (پروفایل کروم مربوط به یک ابزار تست خودکار) و `.codex_visual/` (خروجی‌های png/html میانی طراحی). این‌ها بخشی از کد پروژه نیستند و در `.gitignore` هم نیستند؛ توصیه می‌شود به `.gitignore` اضافه یا حذف شوند (نیازمند تأیید کاربر چون احتمالاً حاوی داده نشست شخصی است).

---

## ۳. نقش‌ها و سلسله‌مراتب سازمانی — بررسی الزام کلیدی کارفرما

نقش‌های تعریف‌شده در `SystemRole.RoleCode` (لایهٔ ORM): `super_admin`, `institution_admin`, `exam_manager`, `teacher`, `teaching_assistant`, `student`, `exam_proctor`, `tech_support`.

**یافته‌های کلیدی دربارهٔ سلسله‌مراتب مدیران آموزشی که کارفرما درخواست کرده:**

- ❌ **هیچ فیلد `parent_manager` یا معادل آن در مدل‌های ORM وجود ندارد.** جست‌وجوی کامل کد برای `parent_manager` فقط در `views.py` (لایهٔ ERD) نتیجه داد، نه در `models.py` یا `forms.py`.
- ⚠️ در لایهٔ **ERD خام**، جدول `academic_manager_scopes` و تابع `_erd_manager_scope_cte()` (خط ۱۱۶۲۰ `views.py`) یک **CTE بازگشتی روی `org_units.parent_id`** پیاده‌سازی کرده که دقیقاً منطق درختی موردنیاز کارفرما (دسترسی مدیر به همهٔ زیرشاخه‌ها) را شبیه‌سازی می‌کند — اما:
  - این ساختار فقط برای دسترسی به **واحد سازمانی (`org_units`)** است، نه برای «مدیر آموزشی زیرمجموعهٔ مدیر آموزشی دیگر» به‌طور مستقیم؛ مدل صریحی برای رابطهٔ «مدیر بالادست ↔ مدیر زیردست» وجود ندارد، بلکه هر دو به یک زیرشاخهٔ سازمانی متصل می‌شوند و تو‌درتو بودن *واحد سازمانی* باعث تو‌درتو شدن دسترسی می‌شود، نه سلسله‌مراتب مستقیم مدیران.
  - این لایه در ORM جنگو **مدل‌سازی نشده** و migration ندارد که Django بشناسد؛ فقط از طریق SQL خام در دسترس است، بنابراین در پنل ادمین جنگو (`admin.py`) و در بقیهٔ ویوهایی که هنوز روی ORM هستند، **اصلاً وجود ندارد**.
- ✅ در لایهٔ ORM، `AcademicUnit` مدل درختی خودارجاع (`parent = ForeignKey('self')`) دارد که می‌تواند مبنای ساختار سازمانی چندسطحی باشد، و `InstitutionAdminProfile.managed_units` (ManyToMany به `AcademicUnit`) وجود دارد — اما این هم رابطهٔ «مدیر به واحد» است، نه «مدیر به مدیر زیردست».
- ❌ **مدیر آموزشی (`exam_manager`) در لایهٔ ORM اصلاً پروفایل اختصاصی ندارد** — بین `SystemAdminProfile`, `InstitutionAdminProfile`, `TeacherProfile`, `StudentProfile`, `ExamProctorProfile`, `TechnicalSupportProfile` هیچ `ExamManagerProfile` وجود ندارد، درحالی‌که `SystemRole.RoleCode.EXAM_MANAGER` تعریف شده و ده‌ها ویو و قالب برای `exam_manager` وجود دارد. عملاً یک نقش کاملاً پیاده‌سازی‌شدهٔ UI است که پشت آن **مدل داده اختصاصی ندارد** (فقط `get_exam_manager_institution()` که مثل `institution_admin` کل یک مؤسسه را برمی‌گرداند، بدون هیچ محدودیت زیرشاخه‌ای).

**نتیجه:** الزام کارفرما مبنی بر «ساختار درختی چندسطحی مدیران آموزشی با `parent_manager`» **در حال حاضر پیاده‌سازی نشده است.** نزدیک‌ترین معادل موجود (CTE روی `org_units` در لایهٔ ERD) هم ناقص، هم موازی با سیستم اصلی، و هم بدون ORM model است. تکمیل این الزام یک **تصمیم معماری مهم** می‌طلبد (بخش «نیازمند تصمیم»).

### کنترل دسترسی سمت سرور (فعلاً چگونه است؟)

✅ **خبر خوب:** کنترل دسترسی *در سرور* انجام می‌شود، نه فقط مخفی‌سازی UI. دکوریتورهای `super_admin_required`, `institution_admin_required`, `exam_manager_required`, `teacher_required`, `assistant_required`, `student_required` (خطوط ۱۳۶۰-۱۵۱۴ `views.py`) روی `login_required` سوار شده و queryset را به `request.managed_institution` محدود می‌کنند.

⚠️ اما دامنهٔ این محدودیت در سطح **یک مؤسسهٔ کامل (Institution)** است، نه زیرشاخهٔ خاص یک مدیر. یعنی هر `exam_manager` یا `institution_admin` به **کل مؤسسهٔ خودش** دسترسی دارد، نه فقط به شاخهٔ سازمانی/زیرمجموعهٔ مدیریتی خودش — دقیقاً همان چیزی که کارفرما صریحاً منع کرده («مدیر آموزشی نباید بتواند مدیران هم‌سطح یا خارج از شاخهٔ خود را ببیند»). تا وقتی مدل «شاخهٔ مدیریتی» در ORM تعریف نشود، این محدودیت قابل پیاده‌سازی درست نیست.

⚠️ لایهٔ ERD یک دکوریتور موازی دیگر هم دارد: `erd_role_required(*allowed_roles)` (خط ۱۱۶۰۳) که کاملاً مستقل از دکوریتورهای بالاست و روی نقش‌های `admin/academic_manager/teacher/student` کار می‌کند — **دو سیستم auth-decorator موازی** در یک فایل.

---

## ۴. وضعیت ماژول‌های ده‌گانهٔ فاز اول

| # | ماژول | وضعیت | توضیح |
|---|--------|--------|-------|
| ۱ | احراز هویت و حساب کاربری | **ناقص** | ورود/خروج/نشست با `SecureLoginView` و `login_2fa` وجود دارد؛ اما باید بررسی شود آیا ۲FA واقعاً کد ارسال/تأیید می‌کند یا فرم صرفاً نمایشی است (نیاز به بررسی عمیق‌تر forms.py/views مربوطه). بازیابی رمز عبور در urls.py **پیدا نشد** (هیچ مسیر password-reset). |
| ۲ | داشبورد | **نسبتاً کامل ولی دوگانه** | داشبورد مدیر آموزشی (نسخهٔ فعال) از داده واقعی SQL خام (ERD) می‌خواند؛ داشبورد super_admin/institution_admin از ORM واقعی می‌خواند. کار می‌کند اما دو منبع داده دارد. |
| ۳ | کاربران | **نسبتاً کامل، نیازمند بازبینی دسترسی** | لیست/جست‌وجو/ورود گروهی (`*_bulk_import`) برای teacher/student/manager در `super_admin` و `exam_manager` وجود دارد. باید تأیید شود ورود گروهی واقعاً ۴ مرحله (فایل→نگاشت→پیش‌نمایش→ثبت) را پیاده کرده یا مستقیم ثبت می‌کند. |
| ۴ | ساختار سازمانی و تنظیمات آموزشی | **ناقص در بخش سلسله‌مراتب مدیران** | `AcademicUnit` (درختی), `AcademicTerm`, `SystemSetting` وجود دارند؛ اما هیچ سازوکار «مدیر بالادست/زیردست» در ORM نیست (بخش ۳). |
| ۵ | درس‌ها و گروه‌بندی | **موجود** | `Course`, `CourseClass` (با `students` M2M) در ORM هست؛ گروه دو-مرحله‌ای (`exam_manager_group_create` + `exam_manager_group_students_add`) در urls.py دیده می‌شود. |
| ۶ | بانک سؤال | **ناقص نسبت به الزامات** | `Question.QuestionType` فقط ۶ نوع دارد: چهارگزینه‌ای، درست/نادرست، جای‌خالی، پاسخ کوتاه، تشریحی، تطبیقی. **دو نوع خواسته‌شده در بریف کارفرما — «چندپاسخی» و «مرتب‌سازی» — در enum مدل وجود ندارند.** همچنین ساختارهای «سناریوی بیمار»، «KFP»، «OSCE» و «Rubric تشریحی» در مدل دیده نشد. |
| ۷ | آزمون‌ها | **کامل نسبتاً خوب** | `Exam` مدل غنی دارد (وضعیت‌های draft/pending_approval/scheduled/active/paused/finished/cancelled، تنظیمات randomize، negative marking، و...). فرایند تأیید (`ExamApproval`) و شروع (`ExamStartAuthorization`) هم مدل دارند. |
| ۸ | پنل استاد | **نسبتاً کامل، آلوده به تکرار** | تعداد زیادی ویو (`teacher_*`) هست اما با تعریف‌های تکراری در سه محل مختلف فایل (بخش ۵). باید بررسی شود کدام نسخه واقعاً فعال است. |
| ۹ | پنل دانشجو و اجرای آزمون | **موجود، با تکرار مشابه** | `StudentExamAttempt`, `StudentQuestionAnswer`, `StudentExamEvent` مدل‌های خوبی برای تایمر/رویداد/پاسخ دارند. اما `student_exam_schedule` و توابع مرتبط هم در فایل views سه بار تکرار شده‌اند. |
| ۱۰ | تقویم آموزشی | **جزئی** | `exam_manager_calendar`, `super_admin_calendar`, `teacher_calendar` وجود دارند اما در حد نمایش لیست آزمون؛ نمایش ماهانه/هفتگی واقعی یا اتصال به رویدادهای ترم (`AcademicTerm.starts_at/ends_at`) تأیید نشد — نیاز به بررسی قالب مربوطه. |

---

## ۵. باگ‌ها، ناسازگاری‌ها و کد تکراری

### ۵.۱. تعریف‌های تابع تکراری در `views.py` (بحرانی)

فایل ۱۶۵۱۵ خطی `apps/core/views.py` شامل تعاریف **هم‌نام تکراری** زیر است (فقط آخرین تعریف در پایتون قابل‌اجراست؛ تعاریف قبلی‌تر **کد مرده کامل** هستند):

- `teacher_panel` — خطوط ۱۰۳۳۱، ۱۱۸۵۰، ۱۲۹۳۵
- `teacher_questions` — خطوط ۱۰۴۹۲، ۱۱۸۸۴، ۱۳۰۵۲
- `student_exam_schedule` — خطوط ۱۰۸۶۹، ۱۲۰۳۱، ۱۵۷۴۴
- `exam_manager_courses` — خطوط ۱۳۵۸۲، ۱۳۹۰۲
- `exam_manager_groups` — خطوط ۱۳۶۴۵، ۱۳۷۴۸
- `exam_manager_dashboard` — خطوط ۱۰۱۵۰، ۱۴۲۳۱، ۱۵۳۴۴
- و مشابه برای `teacher_exams`, `teacher_monitoring`, `teacher_reviews`, `teacher_objections`, `student_attempt`, `student_attempt_submit`, `student_attempt_receipt`, `student_results`, `student_result_detail`, `student_objections`, `exam_manager_group_create`, `exam_manager_group_students_add`, `exam_manager_group_import`, `exam_manager_group_detail`, `exam_manager_approvals`, `exam_manager_proctors`, `exam_manager_active_exams`

این نشان می‌دهد فایل چندین بار به‌صورت **افزوده‌شده (append)** بازنویسی شده بدون حذف نسخهٔ قبلی. تخمین می‌زنم **چند هزار خط** از ۱۶۵۱۵ خط کاملاً غیرقابل‌دسترس (dead code) است. این باعث می‌شود:
- نگهداری/دیباگ فوق‌العاده دشوار شود (۳ نسخه از یک منطق، فقط یکی واقعی).
- ریسک بالا برای تغییرات آینده — ویرایش نسخهٔ اشتباه از یک تابع تکراری هیچ اثری روی رفتار واقعی برنامه نخواهد داشت.

**پیشنهاد برای فاز بعد:** شناسایی دقیق نسخهٔ فعال هرکدام (آخرین تعریف)، حذف نسخه‌های مرده، و تفکیک `views.py` به چند فایل/ماژول به ازای هر نقش (`views/teacher.py`, `views/student.py`, ...). این یک تغییر پرحجم است و باید جدا از فاز اول و با تست دستی کامل هر مسیر انجام شود.

### ۵.۲. دو دکوریتور کنترل دسترسی موازی

`*_required` (ORM-based, خط ۱۳۶۰ به بعد) در برابر `erd_role_required` (ERD-based, خط ۱۱۶۰۳) — دو مکانیزم مجزا با منطق نگاشت نقش متفاوت (`erd_role_code` نگاشت `academic_manager → exam_manager` دارد ولی `teaching_assistant`, `exam_proctor`, `tech_support` در دنیای ERD اصلاً تعریف نشده‌اند).

### ۵.۳. ریسک امنیتی: نقش پیش‌فرض «student»

`erd_primary_role()` (خط ۷۵۰) وقتی پروفایل/نقشی برای کاربر در جدول ERD پیدا نشود، مقدار `'student'` را **پیش‌فرض** برمی‌گرداند به‌جای رد دسترسی یا خطا. اگر مسیری فقط با `erd_role_required('student')` محافظت شده باشد (بدون بررسی وجود پروفایل واقعی)، یک کاربر بدون پروفایل ERD می‌تواند به‌عنوان دانشجو رفتار کند. این باید در فاز اصلاح جدی بررسی شود.

### ۵.۴. اتصال کاربر جنگو به رکورد ERD با تطبیق رشته‌ای

`erd_profile_for_user` کاربر را با `WHERE username = %s OR email = %s` به جدول `profiles` وصل می‌کند، نه با کلید خارجی. اگر دو کاربر ایمیل یکسان (خالی/تکراری) داشته باشند یا username تغییر کند، امکان اتصال به پروفایل اشتباه وجود دارد.

### ۵.۵. دکمه‌ها/لینک‌های بی‌عمل (`href="#"`) و نشانگرهای نمایشی

جست‌وجوی `href="#"` در `templates/` **۱۵۰ مورد در ۴۴ فایل** پیدا کرد (`base.html`, `home.html`, `login.html`, همهٔ قالب‌های `super_admin/*`, `exam_manager/*`, `student/*`, `teacher/modern.html` با ۱۸ مورد). بخشی از این‌ها می‌توانند منوهای dropdown بی‌ضرر (مثل لینک placeholder برای آیتم فعلاً غیرفعال) باشند، اما با توجه به حجم بالا (۱۵۰ مورد) لازم است هرکدام به‌صورت مجزا بررسی شود که آیا:
- باید به یک URL واقعی وصل شوند، یا
- باید به‌وضوح به‌عنوان «به‌زودی / در حال توسعه» علامت‌گذاری شوند (طبق دستور کارفرما، نباید دکمهٔ بی‌عمل بدون توضیح باقی بماند).

### ۵.۶. عدم وجود بازیابی رمز عبور

در `apps/core/urls.py` هیچ مسیری برای فراموشی/بازنشانی رمز عبور (`password_reset`) پیدا نشد. اگر در طراحی محصول چنین قابلیتی مدنظر بوده، باید در فاز بعد اضافه شود؛ در غیر این صورت باید مشخص شود که این ویژگی عمداً حذف شده.

### ۵.۷. بدون تست خودکار

`apps/core` هیچ `tests.py` یا پوشهٔ `tests/` ندارد. سناریوهای دسترسی سلسله‌مراتبی که کارفرما درخواست کرده (مدیر A نمی‌بیند مدیر B را، و...) **در حال حاضر هیچ‌کدام قابل تأیید خودکار نیستند.**

---

## ۶. فایل‌های تکراری / کد و داده بلااستفاده

| مورد | وضعیت | جزئیات |
|------|-------|--------|
| `virtual-exam-starter/` (کل پوشه) | **مشکوک به بلااستفاده** | یک پروژهٔ کامل دیگر (Django+DRF+React+Docker) با `manage.py`, `models.py`, `serializers.py` مجزا از پروژهٔ اصلی؛ در هیچ‌کجای `config/` یا `apps/core` رفرنس داده نمی‌شود. احتمالاً starter/boilerplate اولیهٔ رهاشده. **حذف آن نیازمند تأیید صریح کاربر است** (طبق قانون «حذف فقط پس از تأیید بلااستفاده‌بودن»). |
| `.codex_visual/` | **آرتیفکت طراحی، نه کد** | ده‌ها فایل html/png میانی (نسخه‌های مختلف صفحات managers/students/teachers/dashboard) — به نظر خروجی ابزار طراحی بصری (Codex) است، نه بخشی از اپلیکیشن. untracked است و در `.gitignore` نیست. |
| `.chrome-auth-shot-register/` و `.chrome-auth-shot/` | **پروفایل مرورگر خودکار** | مربوط به ابزار screenshot/تست خودکار (Playwright/Chrome profile)؛ کاملاً غیرمرتبط با سورس‌کد، باید در `.gitignore` باشد. |
| `debug.log`, `dashboard-preview.png`, `login-preview.png` | **فایل توسعه/دیباگ در ریشهٔ ریپو** | `debug.log` علی‌رغم regex `*.log` در `.gitignore`، **track شده** (باید بررسی شود چرا اضافه شده — یا از قبل commit شده و gitignore بعداً اضافه شده). دو png پیش‌نمایش هم در ریشه به‌جای پوشهٔ مستندات/asset هستند. |
| توابع تکراری `views.py` | **کد مرده** | جزئیات در بخش ۵.۱. |
| دو لایهٔ داده (ORM + ERD خام) | **معماری موازی، نه یک فایل خاص** | جزئیات در بخش ۱ و ۳. |

---

## ۷. کیفیت، امنیت و پایداری — یافته‌های سریع

- ✅ رمزهای عبور و اتصال دیتابیس از طریق متغیر محیطی (`os.getenv`) خوانده می‌شوند؛ `SECRET_KEY` مقدار پیش‌فرض ناامن `django-insecure-change-me` دارد که در production باید حتماً override شود (این‌طور در کد هست، فقط باید در `.env` واقعی override شده باشد — قابل بررسی مستقیم نیست چون `.env` در ریپو نیست).
- ✅ `SESSION_COOKIE_SECURE`/`CSRF_COOKIE_SECURE` به‌صورت پویا بر اساس `DEBUG` تنظیم می‌شوند.
- ⚠️ `ALLOWED_HOSTS` مقدار پیش‌فرض شامل یک IP ثابت (`37.32.4.207`) است که در کد hardcode شده — بهتر است فقط از env بیاید.
- ⚠️ به دلیل دو لایهٔ داده، بررسی XSS/تزریق SQL دوچندان مهم است: توابع `erd_*` از پارامترهای positional (`%s`) استفاده می‌کنند که در برابر SQL injection نسبتاً امن است (parametrized)، اما باید در فاز بعد تک‌تک query های ساخته‌شده با f-string بررسی شوند (مثلاً ساخت پویای `sql += f' WHERE {where}'` در `exam_manager_dashboard` — چون `where` از داخل کد کنترل می‌شود نه از ورودی کاربر مستقیم، فعلاً کم‌ریسک است ولی الگوی خطرناکی است که در آینده به‌راحتی می‌تواند به injection منجر شود اگر جایی از ورودی کاربر مستقیماً در `where` استفاده شود).
- ❌ صفحات 404/500 سفارشی (`templates/404.html`, `templates/500.html`) وجود دارند (خوب است) اما باید تأیید شود `handler404`/`handler500` در `config/urls.py` ست شده‌اند.

---

## ۸. تغییراتی که در این مرحله انجام شد

**هیچ.** طبق دستور صریح کارفرما، این مرحله صرفاً بررسی بود. تنها فایل ایجادشده همین `PHASE1_AUDIT.md` است.

---

## ۹. نیازمند تصمیم (قبل از هرگونه پیاده‌سازی)

1. **انتخاب معماری داده:** آیا باید ادامهٔ پروژه بر پایهٔ **ORM جنگو** (`models.py`) پیش برود و لایهٔ ERD خام کنار گذاشته/مهاجرت شود؟ یا برعکس، تصمیم بر این بوده که همه‌چیز به لایهٔ ERD منتقل شود و ORM قدیمی حذف شود؟ **این مهم‌ترین تصمیم پیش‌روی فاز بعد است** چون مستقیماً روی امکان پیاده‌سازی سلسله‌مراتب مدیران آموزشی اثر می‌گذارد.
2. **ساختار سلسله‌مراتب مدیران آموزشی:** آیا `parent_manager` باید مستقیماً بین دو نقش «مدیر آموزشی» تعریف شود (self-referencing FK مثل `AcademicUnit.parent`)، یا باید از طریق تو‌درتویی `AcademicUnit` (که هرکدام یک `manager` دارند) استنتاج شود؟ این دو رویکرد نتایج دسترسی متفاوتی دارند.
3. **حذف یا نگهداری `virtual-exam-starter/`**: آیا این پوشه یک نمونهٔ مرجع عمدی است یا صرفاً باقیمانده و قابل حذف؟
4. **بازیابی رمز عبور**: آیا این قابلیت در طراحی محصول مدنظر بوده و باید اضافه شود، یا عمداً حذف شده (مثلاً چون فقط مدیر می‌تواند رمز کاربران را ریست کند)؟
5. **دو نوع سؤال «چندپاسخی» و «مرتب‌سازی»**: آیا باید به enum مدل `Question.QuestionType` اضافه شوند (نیازمند migration و منطق نمره‌دهی جدید) یا فعلاً از محدودهٔ فاز اول خارج‌اند؟
6. **ساختارهای سناریوی بیمار / KFP / OSCE**: این‌ها در بریف کارفرما به‌عنوان انواع «ساختار سؤال» ذکر شده‌اند اما هیچ مدلی برایشان یافت نشد — آیا این‌ها برای فاز اول ضروری‌اند یا فاز بعد؟
7. **تصمیم دربارهٔ ۱۵۰ مورد `href="#"`**: کدام‌ها باید واقعاً کار کنند و کدام‌ها فقط باید برچسب «در حال توسعه» بگیرند؟ نیاز به فهرست تأییدشده از کارفرما یا بررسی تک‌تک در فاز بعد.
8. **مالکیت `debug.log`, پوشه‌های `.codex_visual/` و `.chrome-auth-shot*`**: آیا صاحب پروژه اجازهٔ حذف/gitignore کردن این موارد را می‌دهد؟

---

## ۱۰. جمع‌بندی برای فاز بعد (پیشنهادی، نه انجام‌شده)

ترتیب پیشنهادی برای فاز پیاده‌سازی (پس از دریافت تصمیم‌های بالا):
1. رفع تصمیم معماری داده (بخش ۹.۱) — بدون این، هیچ تغییر دیگری پایدار نخواهد بود.
2. مدل‌سازی `parent_manager`/سلسله‌مراتب در ORM انتخاب‌شده + migration.
3. اعمال فیلتر دامنهٔ داده در تمام دکوریتورها و querysetها (نه فقط سطح مؤسسه، بلکه سطح زیرشاخهٔ مدیریتی).
4. حذف کد مرده در `views.py` (نسخه‌های تکراری) — به‌صورت تدریجی و با تست دستی هر مسیر.
5. تکمیل انواع سؤال باقیمانده و ساختارهای سناریو در بانک سؤال.
6. بررسی و رفع دکمه‌های بی‌عمل.
7. افزودن حداقل تست‌های دسترسی سلسله‌مراتبی (طبق سناریوهای درخواستی کارفرما).
